/* Preencha com a URL e a chave PUBLICÁVEL do seu projeto Supabase.
   Nunca coloque senha do banco, service_role ou chave secreta neste arquivo. */
window.BANCO_CONFIG = { url: '', chavePublica: '' };
